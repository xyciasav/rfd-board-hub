export async function renderGrants(context){const {api,esc,date,money}=context,root=document.querySelector('#grants-content');root.innerHTML='<div class="data-loading"><span></span><b>Loading grants…</b></div>';const {items}=await api('/api/grants'),active=items.filter(x=>!['Awarded','Declined','Closed'].includes(x.status)),requested=items.filter(x=>x.status==='Submitted').reduce((s,x)=>s+Number(x.amount||0),0),awarded=items.filter(x=>x.status==='Awarded').reduce((s,x)=>s+Number(x.amount||0),0);root.innerHTML=`<div class="section-head"><div><h3>Grant pipeline</h3><p class="muted">Keep opportunities, deadlines, submissions, and awards in one place.</p></div><button id="add-grant">Add grant</button></div><div class="cards"><div class="metric"><span>Active opportunities</span><b>${active.length}</b></div><div class="metric"><span>Submitted</span><b>${money(requested)}</b></div><div class="metric"><span>Awarded</span><b>${money(awarded)}</b></div></div><article>${items.length?`<div class="finance-table"><table><thead><tr><th>Deadline</th><th>Grant</th><th>Funder</th><th>Owner</th><th>Status</th><th>Amount</th><th></th></tr></thead><tbody>${items.sort((a,b)=>String(a.deadline).localeCompare(String(b.deadline))).map(x=>`<tr><td>${date(x.deadline)}</td><td><b>${x.url?`<a href="${esc(x.url)}" target="_blank" rel="noopener">${esc(x.title)}</a>`:esc(x.title)}</b></td><td>${esc(x.funder)}</td><td>${esc(x.owner||'—')}</td><td><span class="pill">${esc(x.status)}</span></td><td>${money(x.amount)}</td><td><div class="row-actions"><button class="table-action" data-edit-grant="${x.id}">Edit</button><button class="table-action danger" data-delete-grant="${x.id}">Delete</button></div></td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">No grants tracked yet.</div>'}</article>`;root.querySelector('#add-grant').onclick=()=>editGrant(null,context);root.querySelectorAll('[data-edit-grant]').forEach(b=>b.onclick=()=>editGrant(items.find(x=>x.id===b.dataset.editGrant),context));root.querySelectorAll('[data-delete-grant]').forEach(b=>b.onclick=async()=>{const item=items.find(x=>x.id===b.dataset.deleteGrant);if(confirm(`Delete “${item.title}”?`)){await api(`/api/grants/${item.id}`,{method:'DELETE'});await renderGrants(context)}})}
function editGrant(item,context){const dialog=document.createElement('dialog');dialog.className='finance-dialog';dialog.innerHTML=`<form><div class="section-head"><div><p class="eyebrow">Grant pipeline</p><h2>${item?'Edit grant':'Add grant'}</h2></div><button type="button" class="icon-button">×</button></div><label>Grant name<input name="title" required value="${context.esc(item?.title||'')}"></label><div class="field-grid"><label>Funder<input name="funder" required value="${context.esc(item?.funder||'')}"></label><label>Potential / requested amount<input name="amount" type="number" min="0" step="0.01" value="${item?.amount||''}"></label></div><div class="field-grid"><label>Deadline<input name="deadline" type="date" value="${item?.deadline||''}"></label><label>Owner<input name="owner" value="${context.esc(item?.owner||'')}"></label></div><label>Status<select name="status">${['Researching','Planning','Writing','Submitted','Awarded','Declined','Closed'].map(x=>`<option ${item?.status===x?'selected':''}>${x}</option>`).join('')}</select></label><label>Grant link<input name="url" type="url" value="${context.esc(item?.url||'')}"></label><label>Notes<textarea name="notes" rows="5">${context.esc(item?.notes||'')}</textarea></label><p class="error"></p><div class="actions"><button type="button" class="secondary">Cancel</button><button>Save grant</button></div></form>`;document.body.append(dialog);const close=()=>{dialog.close();dialog.remove()};dialog.querySelector('.icon-button').onclick=close;dialog.querySelector('.secondary').onclick=close;dialog.querySelector('form').onsubmit=async event=>{event.preventDefault();try{await context.api(item?`/api/grants/${item.id}`:'/api/grants',{method:item?'PUT':'POST',body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget)))});close();await renderGrants(context)}catch(error){dialog.querySelector('.error').textContent=error.message}};dialog.showModal()}
