const fileData=file=>new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.onerror=()=>reject(Error('Could not read the file'));reader.readAsDataURL(file)});
const size=value=>value>1_000_000?`${(value/1_000_000).toFixed(1)} MB`:`${Math.ceil(value/1000)} KB`;

export async function renderMarketingWorkspace({api,esc,date}){
 const root=document.querySelector('#marketing-board');root.innerHTML='<div class="marketing-loading">Loading marketing workspace…</div>';
 const [work,assets,recent]=await Promise.all([api('/api/marketing-workspace'),api('/api/marketing-assets'),api('/api/pdf-jobs')]);
 root.innerHTML=`<div class="marketing-grid"><article class="marketing-tasks"><p class="eyebrow">Vikunja</p><h3>Campaign work</h3><p class="muted">Marketing and advertising tasks from Night of the Living Loud and Rage for Democracy.</p><div class="marketing-task-list">${work.error?`<p class="error">${esc(work.error)}</p>`:!work.configured?'<div class="empty">Add Vikunja credentials in Settings.</div>':work.tasks.length?work.tasks.map(task=>`<a class="marketing-task ${task.done?'done':''}" href="${esc(task.url)}" target="_blank" rel="noopener"><span><b>${esc(task.title)}</b><small>${esc(task.project)} · ${task.labels.map(esc).join(', ')}</small></span><span>${task.done?'Done':date(task.dueDate?.slice(0,10))}</span></a>`).join(''):'<div class="empty">No tasks labeled marketing or advertising were found.</div>'}</div></article>
 <article><p class="eyebrow">Print library</p><h3>Flyers & handouts</h3><form id="asset-upload" class="compact-form"><input name="asset" type="file" accept="image/jpeg,image/png,image/webp,application/pdf" required><button>Upload</button></form><p id="asset-error" class="error"></p><div id="asset-list" class="asset-list">${assets.items.length?assets.items.map(item=>`<a href="/api/marketing-assets/${item.id}/download"><span><b>${esc(item.name)}</b><small>${size(item.size)} · ${new Date(item.uploadedAt).toLocaleDateString()}</small></span><strong>Download</strong></a>`).join(''):'<div class="empty">No printable files uploaded yet.</div>'}</div></article>
 <article class="pdf-builder"><p class="eyebrow">PDF generator</p><h3>Create a donation letter</h3><form id="pdf-form"><div class="field-grid"><label>Business name<input name="businessName" required></label><label>Purpose<input name="purpose" required placeholder="Sponsorship, donation request…"></label></div><div class="field-grid"><label>Document title<input name="title" value="Donation Request"></label><label>Event name<input name="eventName" value="Night of the Living Loud"></label></div><label>Event date<input name="eventDate" type="date"></label><label>Letter details<textarea name="body" rows="6" placeholder="What should the letter say or ask for?"></textarea></label><button>Create PDF</button></form><div id="pdf-progress" class="pdf-progress hidden"><div><span></span></div><p>Preparing your PDF…</p></div><p id="pdf-error" class="error"></p><div class="recent-pdfs"><h3>Recently generated</h3>${recent.items.length?recent.items.map(item=>`<a href="/api/pdf-jobs/${item.id}/download"><span><b>${esc(item.name)}</b><small>${esc(item.businessName||'')} · ${new Date(item.completedAt).toLocaleString()}</small></span><strong>Download</strong></a>`).join(''):'<div class="empty">Generated PDFs will be saved here.</div>'}</div></article></div>`;
 document.querySelector('#asset-upload').onsubmit=async event=>{event.preventDefault();const button=event.currentTarget.querySelector('button'),file=event.currentTarget.elements.asset.files[0],error=document.querySelector('#asset-error');error.textContent='';if(file.size>20_000_000){error.textContent='File must be 20 MB or smaller';return}button.disabled=true;button.textContent='Uploading…';try{await api('/api/marketing-assets',{method:'POST',body:JSON.stringify({name:file.name,mime:file.type,data:await fileData(file)})});await renderMarketingWorkspace({api,esc,date})}catch(x){error.textContent=x.message;button.disabled=false;button.textContent='Upload'}};
 document.querySelector('#pdf-form').onsubmit=async event=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('button'),progress=document.querySelector('#pdf-progress'),bar=progress.querySelector('span'),message=progress.querySelector('p'),error=document.querySelector('#pdf-error');error.textContent='';button.disabled=true;button.textContent='Starting…';progress.classList.remove('hidden');try{const values=Object.fromEntries(new FormData(form)),job=await api('/api/pdf-jobs',{method:'POST',body:JSON.stringify({...values,event_name:values.eventName,event_date:values.eventDate})});const deadline=Date.now()+310000;while(Date.now()<deadline){await new Promise(resolve=>setTimeout(resolve,1000));const current=await api(`/api/pdf-jobs/${job.id}`);bar.style.width=`${current.progress||5}%`;message.textContent=current.status==='generating'?'Generating your PDF…':'Preparing your PDF…';if(current.status==='failed')throw Error(current.error||'PDF generation failed');if(current.status==='ready'){message.textContent='Your PDF is ready. Downloading…';location.href=`/api/pdf-jobs/${job.id}/download`;setTimeout(()=>renderMarketingWorkspace({api,esc,date}),800);return}}throw Error('The generator is still working. Please try again shortly.')}catch(x){error.textContent=x.message}finally{button.disabled=false;button.textContent='Create PDF'}};
}
