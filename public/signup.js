export async function renderSignup(context){const {api,esc}=context,root=document.querySelector('#signup-content'),data=await api('/api/signups');root.innerHTML=`<div class="signup-launch"><div><p class="eyebrow">Tablet-friendly intake</p><h3>Community Sign Up Mode</h3><p>Hand over the tablet without exposing the rest of the Board Hub.</p></div><button id="launch-signup">Launch Sign Up Mode</button></div><article><div class="section-head"><div><h3>Recent signups</h3><p class="muted">${data.items.length} contact${data.items.length===1?'':'s'} saved for your mailing list.</p></div></div>${data.items.length?`<div class="finance-table"><table><thead><tr><th>Name</th><th>Email</th><th>Phone</th><th>Donation interest</th><th>Signed up</th></tr></thead><tbody>${data.items.slice(0,100).map(x=>`<tr><td><b>${esc(x.firstName)} ${esc(x.lastName)}</b></td><td>${esc(x.email)}</td><td>${esc(x.phone||'—')}</td><td>${x.donate?'Yes':'No'}</td><td>${new Date(x.createdAt).toLocaleString()}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">No one has signed up yet.</div>'}</article>`;root.querySelector('#launch-signup').onclick=()=>openSignup(context)}
function openSignup({api,esc}){const overlay=document.createElement('div');overlay.className='signup-mode';overlay.innerHTML=`<button class="signup-exit" type="button">Exit Sign Up Mode</button><div class="signup-brand"><img src="/rfd-logo-color.png" alt="Rage for Democracy"><p class="eyebrow">Join the movement</p><h1>Stay connected.</h1><p>Get updates about community programs, events, and ways to help.</p></div><form class="signup-form"><div class="field-grid"><label>First name<input name="firstName" autocomplete="given-name" required></label><label>Last name<input name="lastName" autocomplete="family-name" required></label></div><label>Email<input name="email" type="email" autocomplete="email" required></label><div class="field-grid"><label>Phone number<input name="phone" type="tel" autocomplete="tel"></label><label>ZIP code<input name="postalCode" autocomplete="postal-code"></label></div><label>What are you interested in?<textarea name="interests" rows="3" placeholder="Events, volunteering, civic education…"></textarea></label><label class="donate-choice"><input name="donate" type="checkbox"> <span><b>I want to donate today</b><small>After signing up, choose a one-time or recurring gift.</small></span></label><label class="consent"><input type="checkbox" required> I agree to receive email updates from Rage for Democracy.</label><p class="error"></p><button class="signup-submit">Sign me up</button></form>`;document.body.append(overlay);overlay.querySelector('.signup-exit').onclick=()=>overlay.remove();overlay.querySelector('form').onsubmit=async event=>{event.preventDefault();const button=event.currentTarget.querySelector('.signup-submit'),payload=Object.fromEntries(new FormData(event.currentTarget));payload.donate=payload.donate==='on';button.disabled=true;button.textContent='Saving…';try{const result=await api('/api/signups',{method:'POST',body:JSON.stringify(payload)});overlay.innerHTML=`<div class="signup-thanks"><img src="/rfd-logo-color.png" alt="Rage for Democracy"><h1>Thank you, ${esc(payload.firstName)}!</h1><p>Your information has been saved.${result.emailWarning?' We could not send the welcome email right now, but your signup is safe.':' Check your inbox for a welcome email.'}</p>${payload.donate?`<div class="donation-actions">${result.donations?.oneTimeUrl?`<a href="${esc(result.donations.oneTimeUrl)}">Make a one-time donation</a>`:''}${result.donations?.recurringUrl?`<a href="${esc(result.donations.recurringUrl)}">Start a recurring donation</a>`:''}${!result.donations?.oneTimeUrl&&!result.donations?.recurringUrl?'<p>Ask the board member for the donation link.</p>':''}</div>`:''}<button id="next-signup">Sign up another person</button><button id="finish-signup" class="secondary">Finish</button></div>`;overlay.querySelector('#next-signup').onclick=()=>{overlay.remove();openSignup({api,esc})};overlay.querySelector('#finish-signup').onclick=()=>overlay.remove()}catch(error){event.currentTarget.querySelector('.error').textContent=error.message;button.disabled=false;button.textContent='Sign me up'}}}
