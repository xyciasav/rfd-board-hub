{
  "name": "rfd-board-hub",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {"start": "node server.js", "check": "node --check server.js && node --check public/app.js && node --check public/insights-wrapper.js && node --check public/marketing-workspace.js", "test": "node --test", "verify": "npm run check && npm test"},
  "engines": {"node": ">=22"}
}
